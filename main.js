function letsAdd(digit1, digit2) {
    return digit1 + digit2;
}

function letsMultiply(num) {
    return num * 2;
}
let thisSum = letsAdd(4, 14);

let ourProduct = letsMultiply(thisSum);

console.log(ourProduct)